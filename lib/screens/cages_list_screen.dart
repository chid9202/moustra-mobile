import 'package:flutter/material.dart';

class CagesListScreen extends StatelessWidget {
  const CagesListScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Cages - List'));
}
