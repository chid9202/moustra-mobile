import 'package:flutter/material.dart';

class CagesGridScreen extends StatelessWidget {
  const CagesGridScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Cages - Grid'));
}
